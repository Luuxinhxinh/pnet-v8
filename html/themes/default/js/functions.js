/* functions.js — RETIRED (Phase 3, Tier A).
 *
 * The 4,672-line monolith was decomposed into ES modules under js/functions/
 * (see functions/index.js). The lab page (index.html) now loads ONLY the
 * type=module entry; nothing loads this file any more.
 *
 * This stub exists solely to override the original monolith that still ships
 * inside the golden customizations tgz (engine-custom is applied last over the
 * tgz, but a deletion in engine-custom cannot remove a golden-layer file — so we
 * neutralise it with this stub instead). Do not add code here; edit the modules
 * under js/functions/ instead.
 */
